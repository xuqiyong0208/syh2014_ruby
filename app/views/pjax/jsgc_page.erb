<div class="main clear">
	<div class="box">
    	<div class="m_title">
        	<h2>当前位置：赛事/竞赛规程</h2>
        </div>
        <div class="m_body">
        	<div class="competition">
            	<div class="rule_list">
                	<ul>
                        <% @projects.each do |project| %>
                        <% xmmc = project[:xmmc].to_s.strip %>
                        <li><a href="/files/jsgc/<%~ URI.escape(xmmc) %>.doc"><img src="<%= static_url "/images/rule_#{project[:dhkey]}.png" %>" alt="" /><p><%~ xmmc %></p></a></li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>